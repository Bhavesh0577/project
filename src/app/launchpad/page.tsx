import StartupLaunchpad from '@/components/launchpad/StartupLaunchpad';

export const metadata = {
  title: 'Hackathon-to-Startup Launchpad',
  description: 'Transform your hackathon project into a viable startup with AI-powered tools',
};

export default function LaunchpadPage() {
  return <StartupLaunchpad />;
} 